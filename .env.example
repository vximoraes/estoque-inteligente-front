# =============================================================================
# CONFIGURAÇÕES DO FRONTEND
# =============================================================================
# Porta em que o frontend irá rodar
PORT=3001

# URL da API para o navegador (client-side)
# Usada em requisições do navegador para a API
NEXT_PUBLIC_API_URL=http://localhost:3010

# URL da API para chamadas server-side (dentro do Docker)
# No Docker, use o nome do serviço (componentes-eletronicos-api) em vez de localhost
API_URL=http://localhost:3010

# =============================================================================
# CONFIGURAÇÕES DO NEXTAUTH
# =============================================================================
# URL base da aplicação
NEXTAUTH_URL=http://localhost:3001

# Chave secreta do NextAuth (use a mesma chave que você usou para gerar os tokens)
# IMPORTANTE: Gere uma chave secreta forte
NEXTAUTH_SECRET=GERE_UMA_CHAVE_SECRETA_AQUI

# Tempo de expiração do JWT Access Token
# Deve ser igual ao configurado no backend (JWT_ACCESS_TOKEN_EXPIRATION)
# Formatos aceitos: s (segundos), m (minutos), h (horas), d (dias)
JWT_ACCESS_TOKEN_EXPIRATION=1h
